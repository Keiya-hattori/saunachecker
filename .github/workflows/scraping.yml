name: Periodic Scraping

on:
  schedule:
    # 30分ごとに実行（UTCタイムゾーン）
    - cron: '*/30 * * * *'
  # 手動実行も可能に
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Run scraping
        run: |
          # アプリのエンドポイントに対してリクエストを送信
          curl -X GET "https://saunachecker.onrender.com/"
          echo "Scraping job completed" 